/**
 * Shared components barrel file.
 *
 * Reusable components that are shared across multiple features/modules
 * (e.g. empty states, loaders, badges) will be exported from here.
 *
 * Example (future use):
 * export { EmptyState } from "./empty-state";
 */

export {};
