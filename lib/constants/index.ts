/**
 * Application-wide constants barrel file.
 *
 * Shared constant values (routes, config keys, enums, etc.) will be exported
 * from here as they are introduced.
 *
 * Example (future use):
 * export const APP_NAME = "AI Social Media Operations Platform";
 */

export {};
